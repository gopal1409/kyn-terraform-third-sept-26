#lets create the git global variables
  git config --global user.name "gopal das"
  git config --global user.email "gopal1409@gmail.com"
  ###to check your global variables
  git config --global --list
##lets create our first commit 
##we will create a folder inside the folder we will create some file
mkdir projectx
cd projectx/
#touch will crete only blank file
touch secret
touch auth
touch DS_Store
touch calcy.py
#inside calcy.py we are adding some line
echo "we are starting with calcy program" > calcy.py
ls
#this will show that git is not managing this folder
git status
#we will insitalized the folder with git 
git init
#now git status will show the folder is managed by git
git status
ls -la
#we will move calcy.py to staging aread
git add calcy.py
git status
#lets do a commit this will create our first version
git commit -m "we are starting with calcy program"
##git log will show the first version
git log

#lets generate the key pair
ssh-keygen -t rsa
##lets add one more line
cat calcy.py
#we will add another line to our calcy project
echo "adding addtion function to calcy.py file" >> calcy.py
#git status will show that the calcy.py file is modified
git status
#if you want to check what modification has been done 
git diff
#lets move the modification to github
git add calcy.py
git commit -m "added addtion function to calc.py"
#now git log will show there are two commit and also you can see the global variable are attache
git log
#finally push the changes to remote once push you can refresh your github page click on calcy.py file it will show two lines
git push origin main
